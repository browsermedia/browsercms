<%= f.input :name %>
<%= f.input :success_url, hint: "Leave blank to send the user to the page they were trying to access" %>
<%= f.input :failure_url, hint: "Leave blank to send the user to the page they tried to login from" %>
<%= f.input :template, as: :template_editor %>
