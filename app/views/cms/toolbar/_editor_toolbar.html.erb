<%
   message = @mode == "edit" ? "On" : "Off"
   button_style = @mode == "edit" ? "btn-success" : ""
%>
<%= content_for :button_bar do %>
    <% if @page.version == @page.draft.version %>
    <% else %>
        <%= menu_button "View Current Version", @page.path, class: 'current_version'%>
    <% end %>
<% end %>
