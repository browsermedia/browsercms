<%= f.input :choices, hint: "Enter each choice on a separate line." %>
<%= f.input :required, hint: "Required fields will show the first option a preselected." %>
<%= f.input :instructions, as: :text %>
