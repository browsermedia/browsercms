<% use_page_title "View Entry" %>
<%= content_for :button_bar, render(partial: 'buttonbar', locals: {view: :show})%>

<% @entry.form.fields.each do |field|  %>
    <b><%=  field.label %>:</b> <%= @entry.data_columns[field.name] %> <br>
<% end %>