<% @page_title = "New Section" %>
<h1><%= @page_title %></h1>
<% form_for(@section, :url => cms_path(:sections, :create, :section_id => @parent)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>