<% content_for :buttons, 'save_buttons' %>
<%= simple_form_for(@redirect) do |f| %>
    <%= render layout: 'form_with_buttons', locals: {f: f} do %>
        <%= f.input :from_path, label: "From" %>
        <%= f.input :to_path, label: "To" %>
    <% end %>
<% end %>
