<%= content_for :button_bar do %>
    <%= render 'buttonbar' %>
<% end %>
<iframe id="page_content" src="<%= url_for(block_path(@block, :inline)) %>" frameborder="0" height="1600px" width="100%"></iframe>
