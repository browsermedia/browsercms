%h1== New #{@block.content_block_label}
- form_for([:cms, @block]) do |f|
  = f.error_messages
  = render :partial => 'cms/blocks/hidden_fields', :locals => {:f => f}
  = render :partial => 'form', :locals => {:f => f}
  .buttons
    = f.submit "Save"