<% @page_title = @toolbar_title = t("users.edit.page_title", :user_login => @user.login) %>

<% content_for :toolbar_links do %>
  <%= link_to(span_tag(t "users.edit.list_all"), cms_users_path, 
    :id => "list_all_button", 
    :class => "button") %>
  <%= link_to(span_tag(t "users.edit.change_password"), [:change_password, :cms, @user], 
    :id => "change_password_button", 
    :class => "button") %>
<% end %>

<% content_for :functions do %> 
  <%= render(:partial => "toolbar") %>
<% end %>

<%= render :partial => 'form' %>
