!!!
%html
  %head
    %title= yield :title
    = stylesheet_link_tag 'cms'
    = javascript_include_tag :defaults    
    = yield :html_head
  %body
    #page
      = cms_toolbar
      #cms_content_library
        %table{:width => '100%', :cellpadding => 5}
          %tr
            %td{:valign=>'top', :width => 240}
              %h2{:style=>'margin-top: 0'} Content Types
              %ul
              - RegisteredBlock.all(:order => "name").each do |block|
                %li= link_to block.name.titleize, send("cms_#{block.name.pluralize.underscore}_path")
            %td{:valign=>'top'}= yield