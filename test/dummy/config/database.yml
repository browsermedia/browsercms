# Only working against development for now.
development:
  adapter: mysql
  encoding: utf8
  reconnect: false
  pool: 5
  database: browsercms_development
  username: root
  password:
  socket: /tmp/mysql.sock

# Testing how static assets serving works.
production:
  adapter: mysql
  encoding: utf8
  reconnect: false
  pool: 5
  database: browsercms_development
  username: root
  password:
  socket: /tmp/mysql.sock

