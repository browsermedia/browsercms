Description:
    Installs the <%= name_of_module %> module.

Example:
    rails generate <%= name_of_module %>:install

    This will:
		1. Copy any migrations from the gem into the project.
		2. Add the routes to the config/routes.rb

