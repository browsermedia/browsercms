Description:
    Installs the given module into your current browsercms project.

Example:
    rails generate cms:install_module bcms_name_of_module

    This will create:
        what/will/it/create
